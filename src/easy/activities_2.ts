// Ejercicio 1: Sumar todos los elementos de un arreglo de números
export function sumarArregloNumeros(arreglo: number[]): number {
    // Implementa el algoritmo para sumar todos los elementos del arreglo
    return 0; // Valor de retorno predeterminado sin resolver
}

// Ejercicio 2: Verificar si un número es primo
export function esPrimo(numero: number): boolean {
    // Implementa el algoritmo para determinar si el número es primo
    return false; // Valor de retorno predeterminado sin resolver
}

// Ejercicio 3: Encontrar el número mayor en un arreglo de números
export function encontrarNumeroMayor(arreglo: number[]): number {
    // Implementa el algoritmo para encontrar el número mayor en el arreglo
    return 0; // Valor de retorno predeterminado sin resolver
}

// Ejercicio 4: Revertir una cadena de texto
export function revertirCadena(cadena: string): string {
    // Implementa el algoritmo para revertir la cadena de texto
    return ''; // Valor de retorno predeterminado sin resolver
}

// Ejercicio 5: Calcular el promedio de un arreglo de números
export function calcularPromedio(arreglo: number[]): number {
    // Implementa el algoritmo para calcular el promedio del arreglo
    return 0; // Valor de retorno predeterminado sin resolver
}
